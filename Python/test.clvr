var a = ["e", "s", "h", "l", "l"]

#squares the number entered
func square(a) {
	print("Square: " + a^2)
}

#cubes the number entered
func cube(a) {
	print("Cube: " + a^3)
}

#gets all combinations of an entered list with 5 elements
func combo(list) {
	cls()
	var words = []

	for i = 0 to 5 {
		for j = 0 to 5 {
			for k = 0 to 5 {
				for l = 0 to 5 {
					for m = 0 to 5 {
						if i != j and i != k and i != l and i != m and j != k and j != l and j != m and k != l and k != m and l != m {
							words + ("" + a/i + a/j + a/k + a/l + a/m)
						}
					}
				}
			}
		}
	}

	return words
}

#squares and cubes numbers 1-5
#for i = 1 to 6 {
#	print("Num: " + i)
#	print(i + input())
#	square(i)
#	cube(i)
#	print()
#}

#var a = combo(a)

#for i = 0 to 10 {
#	var b = "| "
#
#	for j = 0 to 12 {
#		var c = (i*12) + j
#		var b = b + a/c + " | "
#	}
#
#	print(b + "\n")
#}

var a = 5
var b = 5
if a is b {
	print(a + b)
}